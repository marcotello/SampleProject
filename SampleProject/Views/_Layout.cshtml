<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>@ViewBag.Title</title>
</head>
<body>
    <div>Navigation Menu</div>
    <div>
        @await  Component.InvokeAsync("CategoryList", new { filter="a" })
    </div>
    <div>
        @RenderBody()
    </div>
    <div>Footer Section</div>

    <div>
        @RenderSection("MySection", false)
    </div>
</body>
</html>
